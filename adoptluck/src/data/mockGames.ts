import { CoinflipGame } from '../types';

/**
 * No demo fake users or fake bots.
 * Games are created dynamically by real users and saved persistently.
 */
export const INITIAL_GAMES: CoinflipGame[] = [];
